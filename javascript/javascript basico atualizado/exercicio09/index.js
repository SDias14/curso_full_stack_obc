let initialNumber = prompt("Digite um número de 1 a 10");

let initialNumberNumber = Number(initialNumber);


for (let i = 1; i <= 20; i++) {
    console.log(`${initialNumberNumber} x ${i} = ${initialNumberNumber * i}`);
}