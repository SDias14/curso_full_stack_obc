// arrow function

const doubleVelocity = velocity => velocity * 2;
 // quando temos uma arrow function com uma linha  podemos apagar as chaves e o return.podemos tambem omitir os parenteses 

console.log(doubleVelocity(10));